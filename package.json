{
  "name": "@iforge-uos/pharos-edi-client",
  "module": "src/index.ts",
  "type": "module",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iforge-uos/pharos-edi-client.git"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "OP_ACCOUNT=iforge.1password.com op run --no-masking --env-file=.env -- bun run --watch main.ts",
    "regen": "OP_ACCOUNT=iforge.1password.com op run --no-masking --env-file=.env -- bun run generate.ts"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.3",
    "soap": "^1.1.11"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "latest",
    "typescript": "^5.8.3",
    "wsdl-tsclient": "^1.7.1"
  }
}